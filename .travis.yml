language: go

go:
  - "1.12.6"

services:
  - docker

before_install:
  - go get -t -v ./...

install: skip

script:
  - ./build.sh
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)
